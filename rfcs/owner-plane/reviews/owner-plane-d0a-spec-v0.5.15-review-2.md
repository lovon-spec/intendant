# Review 2: D0-A Core + Memory specification v0.5.15

*2026-07-13. Independent review of [`owner-plane-d0a-spec.md`](/Users/vm/owner-plane-d0a-spec.md)
v0.5.15 (4,566 lines, D-1..D-175), against the v0.5.14 synthesized
review's eight hard blockers and one downgraded high. Written without
reading the peer review. Scope: full-row wire verification of all
eight rulings; composed depth limited and stated; freeze judgment the
synthesis's.*

## Executive verdict

**All eight blockers and the D-165 exactness prerequisite are
discharged.** D-168 binds pendency-behind-pending-carriers to every
required-coverage consumer (retirements included, regardless of
strictness) and **mandates** the bounded suffix re-fold for a
late-accepted carrier — the incremental transition v0.5.14 implied
but never obligated. D-169 makes freeze reservation cover every
capable claimant (revivably-quarantined ones reserve too — the
budget-displaced-then-revived trace dies) and types the collision
cause as the conjunction `(winner, freeze_basis)`, invalidating when
**either** member dies. D-170 makes source binding erasure-stable:
the durable attempt records the source-derived leaf hash at
registration, and validation compares against the stored binding once
the source is underivable — closing the availability-dependent
schedule (malicious B′ pending proof, source erased, then proven)
that the family text had promised impossible; the signed-leaf
alternative is rejected for the right reason (an export-authorized
signer could substitute bytes). D-171 gives the journal its
evaluation coordinate (`XferAbort.at`) so a fresh rebuild facing a
later lower-hash sufficient fact re-derives the recorded basis, and
totalizes the cause map. D-172 fixes v0.5.14's self-contradiction
(signing-only chain links were required members its prose
disallowed), rejects a key appearing both retired and terminal, and
states the deep-chain orphaning residual. D-173 names the revocation
wrap domain — **decryptable-wrap**, historical, evaluated at the
completing operation's position, shrunk by prior exclusions and
re-entered by `wrap_add` — resolving the 0-author/65-wrap dispute in
favor of constructibility and voiding the current-membership reading.
D-174 completes the authority predicate to **four axes** (the
published equation had omitted the recover axis its own algebra
defined) and adds certificate×grant temporal compatibility, killing
the resurrected-old-signer trace (revoked G0, renewed C0→C1, zone
legitimately uncovered, fresh G1 — old C0 signed new operations under
absence-as-neutral). D-175 is the round's sharpest: **role-neutral
material identity** — `mat_id = H_mat(SEC1 point bytes)`, the `mat`
domain in the tag inventory — because role-tagged `key_id` gave one
P-256 point two identities (KEM-as-signing defeated device
self-exclusion; signing-as-KEM restored decrypt access);
intra-certificate role reuse rejects; and `w.gen(last_known = H)`
retirement is re-anchored to the effective **accepted** head at or
below H (verbatim retirement was a no-op for held-but-displaced heads
and leaked stale heads into every checkpoint).

**Findings: none in scope.** All eight rulings verified to their
bytes; the `mat` tag, stored-binding rule, conjunction cause,
decryptable-wrap domain, four-axis mirrors, and retirement re-anchor
are all present and consistent.

## Recommendation

Sixteen rounds. The residue is again composition the synthesis's
reducer-level replay finds and the cut structurally closes —
including, this round, a cross-role cryptographic identity bypass and
a resurrected-signer authority hole that no amount of per-ruling
review would have surfaced. The two-review-plus-synthesis loop is the
working verification instrument; the corpus will be its mechanical
successor. If the synthesis concurs that the eight are closed, the
artifact sequence begins: companion schema (opening tranche — the
suffix re-fold cases, conjunction-cause invalidation, stored-binding
erasure schedules, the temporal-compatibility negatives, and the
mat_id cross-role rejections), then the independent core and harness,
corpus, family 14, surfaces, and the discrepancy audit as the Gate-A
decider. Durable P1 writes remain prohibited until Gate B plus the
umbrella's P0.5/tombed-cutover prerequisites, unchanged.
